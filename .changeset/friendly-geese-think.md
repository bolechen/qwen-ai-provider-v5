---
"qwen-ai-provider-v5": patch
---

Improve Qwen 3.5 compatibility and reorganize the provider internals without changing the main public API.

This release also removes noisy stream debug logging and keeps the local rerank script aligned with the refactored source layout.
